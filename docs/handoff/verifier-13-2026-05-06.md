# 이슈 #9 로그인 및 회원가입 기능 개발 - 2차 검증 결과

검증일: 2026-04-29
검증자: verifier

---

## 1. 1차 FAIL 항목 수정 확인

| 항목 | 결과 |
|------|------|
| AuthController - `ApiResponse.created(message)` 적용 | PASS |
| ApiResponse - 문자열 상수화 및 `created(String message)` 팩토리 추가 | PASS |
| JwtConstants - `ROLE_PREFIX` 상수 추가 | PASS |
| JwtAuthenticationFilter - `ROLE_PREFIX` 사용 및 즉시 401 반환 | PASS |
| MemberDetailsService - `ROLE_PREFIX` 사용 | PASS |
| ErrorCode - `INVALID_ROLE` 추가 | PASS |
| AuthServiceImpl - UNKNOWN role 예외 발생 | PASS |
| AuthTestConstants - 토큰 관련 상수 추가 | PASS |
| AuthControllerTest - 하드코딩 제거 | PASS |
| JwtProviderTest - 하드코딩 제거 | PASS |

---

## 2. 신규 발견 FAIL 항목

### FAIL-1: AuthServiceImpl.java 라인 34 - null role 시 NPE 발생
- 파일: `src/main/java/mju/capstone/ddingconnect/domain/auth/service/AuthServiceImpl.java`
- 라인: 34
- 내용: `request.role() == MemberRole.UNKNOWN` 에서 role이 null이면 NPE 발생
- `SignupRequest`에 `@NotNull` 검증 없고 컨트롤러에서 `@Valid`도 없으므로 클라이언트가 role 필드를 생략하면 500 에러 응답
- 수정 방안: `SignupRequest` record에 `@NotNull` 추가 + 컨트롤러 `@Valid` 적용, 또는 null 체크 선행 후 INVALID_ROLE 예외 발생

### FAIL-2: AuthServiceImpl 서비스 레이어 테스트 전무
- 파일: `src/test/java/mju/capstone/ddingconnect/domain/auth/` 하위에 서비스 테스트 없음
- CLAUDE.md "테스트 코드 필수" 규칙 위반
- 검증이 필요한 핵심 분기:
  - UNKNOWN role 회원가입 시 INVALID_ROLE 예외 발생
  - 이메일 중복 회원가입 시 DUPLICATE_EMAIL 예외 발생
  - STUDENT role 회원가입 시 Student 레코드 생성
  - GRADUATE role 회원가입 시 Graduate 레코드 생성
  - 로그인 성공 시 AccessToken 반환
  - 비밀번호 불일치 시 INVALID_CREDENTIALS 예외 발생

---

## 3. WARNING 항목

### WARNING-1: JwtAuthenticationFilter.java 라인 23-24 - JSON 템플릿 하드코딩
- 파일: `src/main/java/mju/capstone/ddingconnect/global/jwt/JwtAuthenticationFilter.java`
- 라인: 23-24
- 내용: `"{\"code\":401,\"message\":\"%s\",\"data\":null}"` JSON 템플릿에 ApiResponse 구조와 401 숫자 리터럴 하드코딩
- ApiResponse 구조 변경 시 불일치 발생 위험

### WARNING-2: ApiResponse.java - `created` 오버로드 모호성
- 파일: `src/main/java/mju/capstone/ddingconnect/global/common/ApiResponse.java`
- 라인: 21-27
- 내용: `created(T data)` 와 `created(String message)` 오버로드 공존으로 향후 String 데이터 반환 시 의도치 않은 메서드 호출 가능성
- 개선 방안: `createdWithMessage(String message)` 등으로 명칭 분리

### WARNING-3: AuthServiceImpl.java - 탈퇴 회원 로그인 차단 없음
- 파일: `src/main/java/mju/capstone/ddingconnect/domain/auth/service/AuthServiceImpl.java`
- 라인: 58-67
- 내용: `isDeleted = true` 인 탈퇴 회원도 로그인 가능. 탈퇴 기능 추가 시 검증 로직 필수

### WARNING-4: JwtAuthenticationFilter.java - CustomException 외 예외 미처리
- 파일: `src/main/java/mju/capstone/ddingconnect/global/jwt/JwtAuthenticationFilter.java`
- 라인: 48
- 내용: `catch (CustomException e)` 만 처리. JwtProvider가 다른 RuntimeException을 던질 경우 500 에러 응답

---

## 최종 판정

**수정 필요**

FAIL 항목 2건 해결 후 재검증 요청 바랍니다.
- FAIL-1: null role NPE 방어 처리
- FAIL-2: AuthServiceImpl 서비스 레이어 테스트 작성



---

# 이슈 #13 SSE 알람 기능 - 검증 결과

검증일: 2026-05-06
검증자: verifier

---

## 1. 하드코딩 검증

### FAIL-1: SseEmitterRepository.java 라인 58 - "ping" 문자열 하드코딩
- 파일: `src/main/java/mju/capstone/ddingconnect/global/sse/SseEmitterRepository.java`
- 라인: 58
- 내용: `emitter.send(SseEmitter.event().comment("ping"))` — "ping" 리터럴이 상수화되지 않음
- CLAUDE.md "하드코딩 절대 금지" 규칙 위반
- 수정 방안: `private static final String HEARTBEAT_COMMENT = "ping";` 상수 선언 후 참조

### WARN-1: SSE_TIMEOUT 동일 값 중복 정의
- 파일 1: `SseServiceImpl.java` 라인 15 — `private static final long SSE_TIMEOUT = 60 * 60 * 1000L;`
- 파일 2: `SseConfig.java` 라인 10 — `private static final long SSE_ASYNC_TIMEOUT = 60 * 60 * 1000L;`
- 동일한 1시간(3600000ms) 값이 두 클래스에 각각 상수로 중복 선언됨
- 향후 한 곳만 수정하면 불일치 발생 위험
- 개선 방안: 공유 상수 클래스(예: SseConstants)로 분리하거나, SseServiceImpl이 SseConfig를 참조

---

## 2. 예외 처리 검증

### PASS: subscribe() IOException 처리
- `SseServiceImpl.subscribe()` 내 초기 connect 이벤트 전송 실패 시 `deleteById()` 호출 확인

### PASS: send() IOException 처리
- `SseServiceImpl.send()` 내 이벤트 전송 실패 시 `deleteById()` 호출 확인

### PASS: sendHeartbeat() IOException 처리
- `SseEmitterRepository.sendHeartbeat()` 내 전송 실패 시 `deleteById()` 호출 확인

### PASS: onCompletion / onTimeout / onError 콜백
- `SseEmitterRepository.save()` 에서 3가지 콜백 모두 `deleteById()` 호출 확인

---

## 3. 테스트 커버리지 검증

### PASS: happy path
- `SseServiceTest.subscribe_emitter_저장_및_반환` — 정상 구독 확인

### PASS: IOException 경로
- `SseServiceTest.subscribe_연결이벤트_전송_실패시_emitter_삭제` — MockedConstruction 활용하여 IOException 경로 검증
- `SseServiceTest.send_전송_실패시_emitter_삭제` — 전송 실패 시 deleteById 호출 확인

### PASS: emitter 없는 경우
- `SseServiceTest.send_emitter_없으면_아무것도_안함` — Optional.empty() 경로 검증

### FAIL-2: SseEmitterRepository 테스트 없음
- `SseEmitterRepository`의 `scheduleHeartbeat`, `sendHeartbeat`, `deleteById` 에 대한 단위 테스트가 전혀 없음
- 특히 heartbeat 전송 실패 시 deleteById 호출 여부, ScheduledFuture 취소 여부는 실제 실행 경로에서 중요한 로직임
- CLAUDE.md "테스트 코드 필수" 규칙 위반

### WARN-2: SseControllerTest 커버리지 불충분
- 현재 `subscribe_성공` 케이스 1개만 존재
- 인증 없는 요청(401 응답) 케이스가 없어 Security 설정 검증 누락

---

## 4. ScheduledFuture 취소 처리 검증

### PASS: deleteById()에서 ScheduledFuture 취소
- `heartbeatTasks.remove(memberId)` 후 `task.cancel(false)` 호출 확인

### WARN-3: ScheduledExecutorService 종료 처리 없음
- `SseEmitterRepository` 내 `scheduler`(ScheduledExecutorService)는 애플리케이션 종료 시 명시적으로 `shutdown()` 되지 않음
- 스프링 컨텍스트 종료 시 스레드 풀이 정리되지 않아 JVM 종료가 지연될 수 있음
- 개선 방안: `@PreDestroy` 메서드에서 `scheduler.shutdown()` 호출

---

## 5. SseConfig async timeout 검증

### PASS
- `SseConfig.configureAsyncSupport()` 에서 `setDefaultTimeout(SSE_ASYNC_TIMEOUT)` 호출 확인

---

## 6. 네이밍 규칙 검증 (camelCase/PascalCase)

### PASS
- 클래스명: `AlarmType`, `SseEmitterRepository`, `SseServiceImpl`, `SseController`, `SseConfig`, `SseTestConstants` — 모두 PascalCase
- 메서드명: `save`, `findById`, `deleteById`, `scheduleHeartbeat`, `sendHeartbeat`, `subscribe`, `send` — 모두 camelCase
- 테스트 메서드명: 한글 설명 방식 사용 (`subscribe_성공`, `send_emitter_존재시_이벤트_전송` 등) — 프로젝트 관례 내 허용

---

## 7. 논리적 오류 검증

### WARN-4: subscribe()에서 save() 반환값 미사용 (잠재적 논리 오류)
- 파일: `SseServiceImpl.java` 라인 24-25
- 내용:
  ```java
  SseEmitter emitter = new SseEmitter(SSE_TIMEOUT);
  sseEmitterRepository.save(member.getId(), emitter);
  ```
- `save()`가 emitter를 그대로 반환하고 있어 현재는 문제 없으나, 향후 `save()`가 래핑된 프록시 emitter를 반환하도록 변경될 경우 로컬 변수 `emitter`와 실제 저장된 emitter가 불일치할 수 있음
- 개선 방안: `SseEmitter emitter = sseEmitterRepository.save(member.getId(), new SseEmitter(SSE_TIMEOUT));` 로 일원화

### WARN-5: 동일 회원 중복 구독 시 기존 emitter 미정리
- 파일: `SseEmitterRepository.java` 라인 23-29
- 내용: `emitters.put(memberId, emitter)` 로 덮어쓸 때 기존 emitter의 `complete()`가 호출되지 않음
- 기존 emitter가 연결된 클라이언트는 서버에서 스트림이 닫히지 않아 클라이언트가 응답을 계속 기다릴 수 있음
- 개선 방안: `put()` 이전에 기존 emitter 조회 후 `complete()` 호출

---

## 최종 판정

**수정 필요**

FAIL 항목 2건 해결 후 재검증 요청 바랍니다.

| 구분 | 항목 | 심각도 |
|------|------|--------|
| FAIL-1 | `SseEmitterRepository` 라인 58 "ping" 문자열 하드코딩 | FAIL |
| FAIL-2 | `SseEmitterRepository` 단위 테스트 없음 | FAIL |
| WARN-1 | SSE_TIMEOUT 동일 값 두 클래스 중복 정의 | WARNING |
| WARN-2 | SseControllerTest 인증 없는 요청 케이스 누락 | WARNING |
| WARN-3 | ScheduledExecutorService @PreDestroy shutdown 없음 | WARNING |
| WARN-4 | subscribe()에서 save() 반환값 미사용 | WARNING |
| WARN-5 | 중복 구독 시 기존 emitter complete() 미호출 | WARNING |

---

# 이슈 #13 SSE 알람 기능 - 3차 검증 결과

검증일: 2026-05-06
검증자: verifier

---

## 1. 2차 FAIL 항목 수정 확인

### FAIL-1 수정 확인 (2차: "ping" 하드코딩)
- 파일: `SseEmitterRepository.java`
- 라인 19: `private static final String HEARTBEAT_COMMENT = "ping";` 상수 선언 확인
- 라인 88: `emitter.send(SseEmitter.event().comment(HEARTBEAT_COMMENT))` 상수 참조 확인
- 결과: PASS

### FAIL-2 수정 확인 (2차: SseEmitterRepository 단위 테스트 없음)
- 파일: `SseEmitterRepositoryTest.java`
- 테스트 케이스 목록:
  - `save_emitter_저장_및_반환` — 저장 및 findById 확인
  - `save_heartbeat_스케줄_등록` — scheduleAtFixedRate 호출 인자 검증
  - `save_중복구독시_기존_emitter_complete_및_task_취소` — completeExisting 동작 검증
  - `findById_존재하는_emitter_반환` — Optional.of 반환 확인
  - `findById_없으면_empty_반환` — Optional.empty 반환 확인
  - `deleteById_emitter_제거_및_task_취소` — ScheduledFuture.cancel(false) 호출 확인
  - `deleteById_없는_id는_예외_없이_종료` — NPE 없이 종료 확인
  - `shutdown_scheduler_종료` — scheduler.shutdown() 호출 확인
- 결과: PASS

### FAIL-1(신규, 2차에서 추가 지적): SseEmitterRepositoryTest 라인 54 `10L` 하드코딩
- 수정 내역: `SseTestConstants.HEARTBEAT_INTERVAL_SECONDS = 10L` 추가
- 파일: `SseTestConstants.java` 라인 14 — `public static final long HEARTBEAT_INTERVAL_SECONDS = 10L;` 선언 확인
- 파일: `SseEmitterRepositoryTest.java` 라인 11 — `import static ... HEARTBEAT_INTERVAL_SECONDS;` 적용 확인
- 라인 57-58 — `eq(HEARTBEAT_INTERVAL_SECONDS)` 2회 참조 확인
- 결과: PASS

---

## 2. 신규 하드코딩 검증

### SseEmitterRepository.java
- 라인 18: `HEARTBEAT_INTERVAL_SECONDS = 10L` — 상수 선언, 참조 일관
- 라인 19: `HEARTBEAT_COMMENT = "ping"` — 상수 선언, 라인 88에서 참조
- 결과: PASS

### SseServiceImpl.java
- 라인 15: `SSE_TIMEOUT = 60 * 60 * 1000L` — 상수 선언
- 라인 16: `CONNECT_EVENT_NAME = "connect"` — 상수 선언
- 라인 17: `CONNECT_MESSAGE = "connected"` — 상수 선언
- 라인 18: `NOTIFICATION_EVENT_NAME = "notification"` — 상수 선언
- 결과: PASS

### SseConfig.java
- 라인 10: `SSE_ASYNC_TIMEOUT = 60 * 60 * 1000L` — 상수 선언
- 결과: PASS

### SseTestConstants.java
- `TEST_MEMBER_ID = 1L`, `TEST_EMAIL`, `TEST_NICKNAME`, `TEST_PASSWORD`, `SUBSCRIBE_URL`, `TEST_ALARM_CONTENT`, `HEARTBEAT_INTERVAL_SECONDS` 모두 상수화
- 결과: PASS

### SseControllerTest.java
- 모든 값이 `SseTestConstants.*` import 정적 참조로 처리됨
- 결과: PASS

### SseServiceTest.java
- 모든 값이 `SseTestConstants.*` import 정적 참조로 처리됨
- 결과: PASS

---

## 3. 예외 처리 검증 (변경 없음, 재확인)

| 항목 | 결과 |
|------|------|
| subscribe() IOException → deleteById() | PASS |
| send() IOException → deleteById() | PASS |
| sendHeartbeat() IOException → deleteById() | PASS |
| onCompletion/onTimeout/onError 콜백 → deleteById() | PASS |
| completeExisting() 기존 emitter complete() 및 task cancel() | PASS |

---

## 4. 테스트 커버리지 검증

### SseServiceTest (변경 없음, 재확인)
- subscribe 성공, subscribe IOException, send 성공, send 없는 emitter, send IOException 실패 — 5개 케이스 모두 PASS

### SseEmitterRepositoryTest (신규)
- 총 8개 케이스 존재, 핵심 경로 전수 커버 확인
- 결과: PASS

### SseControllerTest
- subscribe_성공 1개 케이스 존재
- WARN-2(이월): 인증 없는 요청 케이스 없음 — SecurityConfig가 anyRequest().permitAll() 상태이므로 401 동작 보장 불가. FAIL 아님

---

## 5. 논리적 오류 검증 (이월 WARNING 재확인)

### WARN-4 재확인: subscribe()에서 save() 반환값
- `SseServiceImpl.java` 라인 24: `SseEmitter emitter = sseEmitterRepository.save(member.getId(), new SseEmitter(SSE_TIMEOUT));`
- 3차 코드 기준으로 확인 시 `save()` 반환값을 직접 변수에 대입하는 형태로 구현됨
- 결과: PASS (2차 지적 사항 반영 확인)

### WARN-5 재확인: 중복 구독 시 기존 emitter complete()
- `SseEmitterRepository.java` `completeExisting()` 메서드가 `save()` 내에서 먼저 호출됨 (라인 34)
- 기존 emitter `complete()` 및 heartbeat task `cancel()` 처리 확인
- 결과: PASS (2차 지적 사항 반영 확인)

### WARN-3 재확인: ScheduledExecutorService @PreDestroy
- `SseEmitterRepository.java` 라인 56-58: `@PreDestroy public void shutdown()` 구현 확인
- 결과: PASS (2차 지적 사항 반영 확인)

---

## 6. CLAUDE.md 규칙 위반 검증

| 규칙 | 결과 |
|------|------|
| 하드코딩 절대 금지 | PASS — 문자열/숫자 리터럴 전부 상수 처리 |
| 테스트 코드 필수 | PASS — SseServiceTest, SseEmitterRepositoryTest, SseControllerTest 존재 |
| 테스트 코드에서도 하드코딩 금지 | PASS — SseTestConstants 참조 일관 |
| 함수명 camelCase | PASS |
| 클래스명 PascalCase | PASS |

---

## 7. 이월 WARNING 현황 (FAIL 아님)

| 구분 | 내용 | 상태 |
|------|------|------|
| WARN-1 | SSE_TIMEOUT 두 클래스 중복 정의 | 이월 유지 |
| WARN-2 | SseControllerTest 인증 없는 요청 케이스 없음 | 이월 유지 (SecurityConfig 구조상 한계) |

---

## 최종 판정

검증 통과

3차 검증 기준 FAIL 항목 없음.
2차에서 지적된 FAIL-1("ping" 하드코딩), FAIL-2(SseEmitterRepositoryTest 미존재), FAIL-1신규(HEARTBEAT_INTERVAL_SECONDS 하드코딩) 모두 수정 완료 확인.
이월 WARNING 2건은 현재 프로젝트 구조상 FAIL 기준에 해당하지 않음.
