plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.13'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'mju.capstone'
version = '0.0.1-SNAPSHOT'
description = 'ddingconnect'

// 빌드 디렉터리는 -PbuildDir=... 또는 gradle.properties 의 buildDir 로 외부화한다.
// OneDrive 동기화 잠금 방지가 필요한 환경(Windows)만 외부 경로를 지정하고,
// 미지정 시 Gradle 기본값(build/)을 사용해 리눅스/맥/CI 에서도 그대로 빌드된다.
def externalBuildDir = providers.gradleProperty("buildDir").getOrNull()
if (externalBuildDir != null) {
    layout.buildDirectory = file(externalBuildDir)
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.boot:spring-boot-starter-web'
    compileOnly 'org.projectlombok:lombok'
    runtimeOnly 'com.mysql:mysql-connector-j'
    annotationProcessor 'org.projectlombok:lombok'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testCompileOnly 'org.projectlombok:lombok'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    testAnnotationProcessor 'org.projectlombok:lombok'
// .env
    implementation 'me.paulschwarz:spring-dotenv:4.0.0'
// Spring Security
    implementation 'org.springframework.boot:spring-boot-starter-security'
    testImplementation 'org.springframework.security:spring-security-test'
// JWT
    implementation 'io.jsonwebtoken:jjwt-api:0.12.6'
    runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.12.6'
    runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.12.6'

// swagger
    implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.7.0'
// validation
    implementation 'org.springframework.boot:spring-boot-starter-validation'

// email verify
    implementation 'org.springframework.boot:spring-boot-starter-mail'


// Redis
    implementation 'org.springframework.boot:spring-boot-starter-data-redis'
    implementation 'org.springframework.session:spring-session-data-redis'



    // s3
    implementation 'software.amazon.awssdk:s3:2.25.61'

}

tasks.named('test') {
    useJUnitPlatform()
}
